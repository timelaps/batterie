module.exports = function (prehere_) {
    // var prehere = prehere_ || 0;
    // var total = prehere + 1 + 1;
    // // var err = new Error();
    // var stack = err.stack;
    // var split = stack.split('\n');
    // var parsed = parser(split, total);
    // console.log(parsed);
};
// function parser(string) {
//     return string;
// }